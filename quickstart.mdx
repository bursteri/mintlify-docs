---
title: "Quickstart"
description: "Set up your workspace, collect your first website signal, and choose your next PlainRouter workflow."
---

<Info>
  Registration is open and requires no contact-sales form. Review [pricing](https://plainrouter.com/pricing) for the Free band's spend threshold, arrival allowance, and overage rules. After workspace and Meta ad-account setup, the workspace owner can manage production tokens from **Account** → **API**.
</Info>

Welcome to PlainRouter. This guide takes you from a new account to the first real arrival through your first-party Signals domain.

<Card title="What you will accomplish" icon="circle-check">
  Create your account, connect a first-party collection domain, install the generated browser snippet, and observe the first arrival.
</Card>

## Before you begin

Before you begin, you need:

- An email address you can access.
- Access to the DNS provider for your website domain.
- Access to your website's shared `<head>` template or site-wide code injection setting.
- A consent integration for conversion events and advertising delivery. The identity-free arrival check does not wait for a consent grant.

## Set up your account

<Steps>
  <Step title="Create your account">
    [Create a PlainRouter account](https://plainrouter.com/register).

    PlainRouter creates a personal team and a default workspace, then opens the Signals setup flow. A workspace keeps one client, brand, or market and its connected resources together.

    Connecting an ad account is optional. If you open ad-account onboarding, **Continue** takes you to Signals setup even without a connected account; **Skip** takes you to the dashboard.
  </Step>
  <Step title="Name your workspace">
    Open the [PlainRouter dashboard](https://plainrouter.com/dashboard), then go to **Settings** → **Workspace**.

    Give the workspace a name your team will recognize, such as the client, brand, or market name. You can create and switch workspaces later when you need to separate access or data.
  </Step>
</Steps>

## Set up Signals

Signals collects first-party conversion events from your website through a collection subdomain you control.

<Steps>
  <Step title="Enter your website address">
    Open **Signals** and continue setup. In **Website address**, enter the website where you will install Signals, such as `www.example.com`.

    The form supplies `https://`. Use the exact website host, without a page path, query string, or fragment. The website address identifies the site allowed to send events; it is separate from your collection subdomain, such as `collect.example.com`.
  </Step>
  <Step title="Continue to collection-domain setup">
    Follow the setup flow to the collection-domain and DNS instructions. Meta connection and dataset selection are separate from installing first-party Signals.

    Use the collection domain displayed by PlainRouter. When entering a collection subdomain, use a hostname such as `collect.example.com`, without `https://` or a path.
  </Step>
  <Step title="Add the CNAME record">
    Copy the CNAME name and target from PlainRouter into your DNS provider. Keep the setup screen open or return to it from **Signals**.

    CNAME detection and automatic HTTPS validation can take several minutes. If setup remains pending, click **Retry validation** after your provider has published the change.

    See [Configure DNS and CNAME](/signals/configure-dns) for provider field guidance and troubleshooting.
  </Step>
  <Step title="Install Signals and wait for an arrival">
    When the collection domain becomes active, copy the generated snippet into the shared `<head>` of your site, deploy, and visit a page. The snippet sends one identity-free arrival before evaluating consent.

    PlainRouter completes setup after the first qualifying arrival is stored for the workspace. The setup screen polls automatically, backs off while it waits, and resumes after you return to the tab. Connect your consent flow separately before sending conversion events or advertising identity.

    See [Install the Signals pixel](/signals/install-pixel) for the snippet and consent requirements.
  </Step>
</Steps>

For server-side API, SDK, or CLI access, use the **Workspace API secret** panel in Signals setup. If you need a new secret, choose **Rotate API secret**, copy the replacement into server-side credential storage, and select **Confirm replacement installed** after updating your integration. The secret is shown once. The browser snippet uses the publishable key and must never contain this secret.

Managed-hostname setup can also recognize qualifying verified server revenue. The identity-free `POST /api/v1/verification-events` operation remains available as a server-side diagnostic, but it does not count as real arrival or revenue evidence and does not complete onboarding. See [Test Signal ingestion](/signals/verify-ingestion).

<Note>
  Signal setup is optional and resumable. Closing the setup screen does not remove your collection domain or log you out.
</Note>

## Confirm your setup

Your initial PlainRouter setup is complete when:

- **Signals** shows setup as complete after receiving the first qualifying arrival on an active collection domain.
- Your workspace name clearly identifies the client, brand, or market it contains.

You can now return to the [dashboard](https://plainrouter.com/dashboard) and continue working from the workspace you created.

The dashboard's customizable charts are labeled **Dashboard preview · Sample data**. Use **Signals** to confirm actual collection, and [billing setup](/billing/overview) to interpret a checkout notice.

## Continue from here

Choose the next step that matches your goal:

<CardGroup cols={2}>
  <Card title="Choose an SDK or CLI" icon="boxes" href="/sdk/overview">
    Add server-side conversions to your application or inspect events from a terminal.
  </Card>
  <Card title="Track conversion events" icon="mouse-pointer-click" href="/signals/track-events">
    Add purchases, leads, checkouts, and other events to your implementation.
  </Card>
  <Card title="Review browser measurement" icon="code" href="/signals/install-pixel">
    Review the installed Signals pixel and troubleshoot consent-aware website events.
  </Card>
  <Card title="Connect Meta" icon="plug" href="/signals/connect-meta">
    Send paired browser Pixel and server events with automatic deduplication.
  </Card>
  <Card title="Read signal health" icon="activity" href="/signals/health-and-performance">
    Learn when measurement is healthy, pending, or needs attention.
  </Card>
  <Card title="Connect an agent" icon="bot" href="/actions/connect-agent">
    Authorize one ad account for a compatible MCP client.
  </Card>
</CardGroup>
