---
title: "Quickstart"
description: "Set up your workspace, collect your first website signal, and choose your next PlainRouter workflow."
---

<Info>
  PlainRouter's complete control plane costs &#36;0 while combined monthly managed ad spend is under &#36;300. Registration is open and requires no contact-sales form. After workspace and Meta ad-account setup, the workspace owner can manage production tokens from **Account** → **API**.
</Info>

Welcome to PlainRouter. This guide takes you from a new account to the first real arrival through your first-party Signals domain.

<Card title="What you will accomplish" icon="circle-check">
  Create your account, connect a first-party collection domain, install the generated browser snippet, and observe the first consented arrival.
</Card>

## Before you begin

Before you begin, you need:

- An email address you can verify.
- Access to the DNS provider for your website domain.
- Access to your website's shared `<head>` template or site-wide code injection setting.
- A consent integration that can provide PlainRouter with the required advertising permissions.

## Set up your account

<Steps>
  <Step title="Create your account">
    [Create a PlainRouter account](https://plainrouter.com/register), then verify your email address.

    PlainRouter creates a personal team and a default workspace, then opens the Signals setup flow. A workspace keeps one client, brand, or market and its connected resources together.
  </Step>
  <Step title="Name your workspace">
    Open the [PlainRouter dashboard](https://plainrouter.com/dashboard), then go to **Settings** → **Workspace**.

    Give the workspace a name your team will recognize, such as the client, brand, or market name. You can create and switch workspaces later when you need to separate access or data.
  </Step>
</Steps>

## Set up Signals

Signals collects first-party conversion events from your website through a collection subdomain you control.

<Steps>
  <Step title="Start your first signal">
    In the dashboard, open **Signals** and click **New signal** or **Set up your first signal**.

    Enter a collection subdomain that you control, such as `collect.example.com`. Do not include `https://`.
  </Step>
  <Step title="Copy the workspace API secret">
    PlainRouter shows the newly issued Signals workspace secret once, directly in the setup screen. Copy it into your server-side secret manager before leaving the page.

    The secret is not stored as plaintext and cannot be shown again. If you miss it, issue a replacement from the same screen.
  </Step>
  <Step title="Add the CNAME record">
    Copy the CNAME name and target from PlainRouter into your DNS provider. Keep the setup screen open or return to it from **Signals**.

    CNAME detection and automatic HTTPS validation can take several minutes. If setup remains pending, click **Retry validation** after your provider has published the change.

    See [Configure DNS and CNAME](/signals/configure-dns) for provider field guidance and troubleshooting.
  </Step>
  <Step title="Install Signals and wait for an arrival">
    When the collection domain becomes active, copy the generated snippet into the shared `<head>` of your site. Connect it to your consent flow, deploy, and visit a page where all required advertising permissions are granted.

    PlainRouter completes setup after the first qualifying `PageView` reaches the workspace. The setup screen polls automatically, backs off while it waits, and resumes after you return to the tab.

    See [Install the Signals pixel](/signals/install-pixel) for the snippet and consent requirements.
  </Step>
</Steps>

The identity-free `POST /api/v1/verification-events` operation remains available as a server-side diagnostic, but it does not count as real arrival evidence and does not complete onboarding. See [Test Signal ingestion](/signals/verify-ingestion).

<Note>
  Signal setup is optional and resumable. Closing the setup screen does not remove your collection domain or log you out.
</Note>

## Confirm your setup

Your initial PlainRouter setup is complete when:

- **Signals** shows setup as complete after receiving the first qualifying arrival on an active collection domain.
- Your workspace name clearly identifies the client, brand, or market it contains.

You can now return to the [dashboard](https://plainrouter.com/dashboard) and continue working from the workspace you created.

## Continue from here

Choose the next step that matches your goal:

<CardGroup cols={2}>
  <Card title="Track conversion events" icon="mouse-pointer-click" href="/signals/track-events">
    Add purchases, leads, checkouts, and other events to your implementation.
  </Card>
  <Card title="Review browser measurement" icon="code" href="/signals/install-pixel">
    Review the installed Signals pixel and troubleshoot consent-aware website events.
  </Card>
  <Card title="Connect Meta" icon="plug" href="/signals/connect-meta">
    Send paired browser Pixel and server events with automatic deduplication.
  </Card>
  <Card title="Read signal health" icon="activity" href="/signals/health-and-performance">
    Learn when measurement is healthy, pending, or needs attention.
  </Card>
  <Card title="Connect an agent" icon="bot" href="/actions/connect-agent">
    Authorize one ad account for a compatible MCP client.
  </Card>
</CardGroup>
